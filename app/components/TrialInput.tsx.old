'use client';

import { useState } from 'react';
import { Loader2, AlertCircle } from 'lucide-react';

const HUNTER_EXAMPLE =
  'Hunteria umbellata trial with 60 adults with early type 2 diabetes, 12 weeks follow up, HbA1c primary endpoint, single active arm vs lifestyle advice. Expect 0.8% HbA1c reduction vs baseline.';

export function TrialInput() {
  const [text, setText] = useState(HUNTER_EXAMPLE);
  const [loading, setLoading] = useState(false);
  const [trial, setTrial] = useState<{n_patients: number, weeks: number, endpoint: string} | null>(null);
  const [error, setError] = useState<string | null>(null);

  async function handleRun() {
    try {
      setError(null);
      setLoading(true);
      
      // Mock parsing logic
      const parsedTrial = {
        n_patients: 60,
        weeks: 12,
        endpoint: 'HBA1C'
      };
      
      // Simulate API delay
      await new Promise(resolve => setTimeout(resolve, 1000));
      
      setTrial(parsedTrial);
      console.log('Simulation started for:', text);
    } catch (e: any) {
      setError(e.message || 'Something went wrong');
    } finally {
      setLoading(false);
    }
  }

  return (
    <section className="bg-slate-900/70 border border-slate-800 rounded-2xl p-6 shadow-xl">
      <h2 className="text-lg font-semibold text-slate-100 mb-2">
        Paste a trial protocol
      </h2>
      <p className="text-sm text-slate-400 mb-4">
        Example: &quot;Hunteria umbellata, 60 patients, 12 weeks, HbA1c endpoint&quot;.
      </p>

      <textarea
        value={text}
        onChange={(e) => setText(e.target.value)}
        rows={6}
        disabled={loading}
        className="w-full rounded-xl bg-slate-950 border border-slate-700 px-3 py-2 text-sm text-slate-100 disabled:opacity-60 disabled:cursor-not-allowed transition-colors"
        placeholder="Describe your clinical trial protocol..."
      />

      <div className="mt-4 flex flex-wrap gap-3 items-center">
        <button
          onClick={handleRun}
          disabled={loading || !text.trim()}
          className="inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-gradient-to-r from-cyan-500 to-blue-500 text-slate-950 text-sm font-semibold hover:from-cyan-400 hover:to-blue-400 disabled:opacity-60 disabled:cursor-not-allowed transition-all duration-200"
        >
          {loading ? (
            <>
              <Loader2 className="h-4 w-4 animate-spin" />
              Simulating...
            </>
          ) : (
            'Run Simulation'
          )}
        </button>
        
        <button
          type="button"
          onClick={() => setText(HUNTER_EXAMPLE)}
          disabled={loading}
          className="text-xs text-slate-400 hover:text-slate-200 disabled:opacity-50"
        >
          Use Hunteria example
        </button>
        
        {trial && (
          <p className="text-xs text-slate-400">
            Parsed: {trial.n_patients} pts · {trial.weeks} weeks ·{' '}
            {trial.endpoint}
          </p>
        )}
      </div>

      {error && (
        <div className="mt-3 flex items-center gap-2 p-3 bg-rose-900/30 border border-rose-800 rounded-lg">
          <AlertCircle className="h-4 w-4 text-rose-400" />
          <p className="text-sm text-rose-300 font-medium">{error}</p>
        </div>
      )}

      {/* Results Section - Simplified */}
      {trial && (
        <div className="mt-8 space-y-6">
          <div className="grid md:grid-cols-2 gap-6">
            <div className="bg-slate-900/60 border border-slate-800 rounded-xl p-4">
              <h3 className="text-sm font-semibold text-slate-200 mb-2">
                Power & Dropouts
              </h3>
              <p className="text-3xl font-bold text-cyan-300 mb-1">
                98% <span className="text-base">power</span>
              </p>
              <p className="text-sm text-slate-400">
                Avg dropouts (Monte Carlo):{' '}
                <span className="text-slate-100 font-semibold">6</span>
              </p>
              <p className="text-sm text-slate-400">
                Participants per arm:{' '}
                <span className="text-slate-100 font-semibold">30</span>
              </p>
              
              <div className="mt-4">
                <label className="block text-xs font-medium text-slate-400">
                  Dropout rate (10%)
                </label>
                <input
                  type="range"
                  min={0}
                  max={0.4}
                  step={0.05}
                  defaultValue={0.1}
                  className="w-full mt-1"
                />
              </div>
            </div>

            <div className="bg-slate-900/60 border border-slate-800 rounded-xl p-4">
              <h3 className="text-sm font-semibold text-slate-200 mb-2">
                CONSORT Flow (synthetic)
              </h3>
              <div className="h-48 flex items-center justify-center text-slate-400">
                <div className="text-center">
                  <div className="text-3xl mb-2">📊</div>
                  <p className="text-sm">Visualization placeholder</p>
                  <p className="text-xs mt-2">Assessed → Randomized → Completed → Analyzed</p>
                </div>
              </div>
            </div>
          </div>

          <div className="bg-slate-900/40 border border-slate-800 rounded-xl p-4">
            <h4 className="text-sm font-semibold text-slate-200 mb-3">
              CONSORT Summary
            </h4>
            <div className="grid grid-cols-4 gap-3">
              <div className="text-center">
                <p className="text-2xl font-bold text-cyan-300">72</p>
                <p className="text-xs text-slate-400">Assessed</p>
              </div>
              <div className="text-center">
                <p className="text-2xl font-bold text-cyan-300">60</p>
                <p className="text-xs text-slate-400">Randomized</p>
              </div>
              <div className="text-center">
                <p className="text-2xl font-bold text-cyan-300">54</p>
                <p className="text-xs text-slate-400">Completed</p>
              </div>
              <div className="text-center">
                <p className="text-2xl font-bold text-cyan-300">51</p>
                <p className="text-xs text-slate-400">Analyzed</p>
              </div>
            </div>
          </div>
        </div>
      )}
    </section>
  );
}